bcr_test_module:
  module_path: "tests"
  matrix:
    platform: ["debian10", "macos", "macos_arm64", "ubuntu2004"]
    bazel: ["6.x", "7.x"]
  tasks:
    run_tests:
      name: "Run test module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
